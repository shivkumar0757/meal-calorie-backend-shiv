# USDA FoodData Central API Configuration
USDA_API_KEY=your_usda_api_key_here_from_fdc_nal_usda_gov

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/calory_counter

# JWT Authentication Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Rate Limiting Configuration (Original Requirement: 15 requests per minute from same IP)
API_RATE_LIMIT=15

# Environment Settings
ENVIRONMENT=dev

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Instructions:
# 1. Copy this file to .env
# 2. Get your USDA API key from: https://fdc.nal.usda.gov/api-key-signup.html
# 3. Replace placeholder values with your actual configuration
# 4. Never commit the .env file to version control
